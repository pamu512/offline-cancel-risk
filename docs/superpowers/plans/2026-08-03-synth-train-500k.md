# Synthetic 500k Train Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sharded full-assess synthetic training (~500k) producing Phase A (scenario labels) then Phase B (noisy teacher) joblib shadow bundles.

**Architecture:** Scenario generators build `(AssessRequest, GPS, labels)` → `assess_order` with a feature sink → shard `.npz` + manifest → `MultiOutputRegressor` train → directory bundle → optional registry sideload. CLI `scripts/train_synth.py` drives overnight runs with resume.

**Tech Stack:** Python 3.11+, numpy, scikit-learn, joblib, existing `assess_order` / `FakeGpsClient` / `load_bundle`

## Global Constraints

- Full `assess_order` path (not feature-table-only synth)
- Phase A labels from scenario table only; Phase B = flags ⊕ independent flip rate `ε` (default 0.05)
- Feature keys exactly: `final_stop_confidence`, `sequence_score`, `dwell_fraction`, `abuse_score`, `theft_score`
- Bundle uses `MultiOutputRegressor` + `.predict()` (no `predict_proba`)
- Shards: `shard_XXXX.npz` + `manifest.json`; no new deps (no parquet)
- No auto-promote to champion; shadow sideload only
- Do not claim synth metrics as production lift
- Commits only when the human asks (skip plan commit steps unless requested)

## File structure

| Path | Responsibility |
|---|---|
| `src/offline_cancel_risk/train/__init__.py` | Package export |
| `src/offline_cancel_risk/train/scenarios.py` | Templates, weights, request+GPS builders, Phase A labels |
| `src/offline_cancel_risk/train/labels.py` | Phase B noisy-teacher flip |
| `src/offline_cancel_risk/train/shards.py` | Manifest, npz write/load, resume |
| `src/offline_cancel_risk/train/dataset.py` | Generate+assess one shard |
| `src/offline_cancel_risk/train/fit.py` | Train regressor + emit bundle |
| `scripts/train_synth.py` | CLI |
| `tests/test_train_synth.py` | Unit + tiny end-to-end |
| `docs/OPS.md` | Overnight run blurb |
| `src/offline_cancel_risk/pipeline/assess.py` | Optional `feature_sink` |
| `src/offline_cancel_risk/pipeline/score_build.py` | Export `ML_FEATURE_KEYS` |

---

### Task 1: Export ML feature keys + assess feature sink

**Files:**
- Modify: `src/offline_cancel_risk/pipeline/score_build.py`
- Modify: `src/offline_cancel_risk/pipeline/assess.py`
- Test: `tests/test_train_synth.py`

**Interfaces:**
- Produces: `ML_FEATURE_KEYS: tuple[str, ...]` from `score_build`
- Produces: `assess_order(..., feature_sink: dict[str, float] | None = None)` fills sink with `ctx.ml_feature_vec` after score stage

- [ ] **Step 1: Write failing test**

```python
# tests/test_train_synth.py
import pytest
from offline_cancel_risk.adapters.gps import FakeGpsClient
from offline_cancel_risk.adapters.publishers import JsonlStreamPublisher, SqliteTablePublisher
from offline_cancel_risk.api.schemas import AssessRequest
from offline_cancel_risk.domain.models import GpsPoint
from offline_cancel_risk.pipeline.assess import assess_order
from offline_cancel_risk.pipeline.score_build import ML_FEATURE_KEYS
from offline_cancel_risk.settings import load_policy
from pathlib import Path
from datetime import datetime, timedelta

PICKUP = (14.55, 121.02)

def _points(n=20):
    base = datetime(2024, 1, 1, 10, 0, 0)
    return [
        GpsPoint(lat=PICKUP[0], lon=PICKUP[1],
                 ts=(base + timedelta(minutes=i)).strftime("%Y-%m-%d %H:%M:%S"),
                 speed_mps=0.2)
        for i in range(n)
    ]

@pytest.mark.asyncio
async def test_assess_feature_sink(tmp_path: Path):
    assert ML_FEATURE_KEYS == (
        "final_stop_confidence", "sequence_score", "dwell_fraction",
        "abuse_score", "theft_score",
    )
    sink: dict[str, float] = {}
    req = AssessRequest(
        order_display_id="SINK-1", driver_id=1,
        cancel_ts="2024-01-01 11:20:00", assign_ts="2024-01-01 10:00:00",
        latlong=f"{PICKUP[0]}|{PICKUP[1]},14.65|121.08",
        path_point_num=2, order_status="CANCELLED", category="FOOD",
        order_value=100.0, currency="PHP",
    )
    await assess_order(
        req, FakeGpsClient(_points()),
        load_policy("config/policy.default.yaml"),
        stream=JsonlStreamPublisher(stream_path=tmp_path / "e.jsonl"),
        table=SqliteTablePublisher(sqlite_path=tmp_path / "a.db"),
        feature_sink=sink,
    )
    assert set(ML_FEATURE_KEYS) <= set(sink)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_train_synth.py::test_assess_feature_sink -v`  
Expected: FAIL (`ML_FEATURE_KEYS` or `feature_sink` missing)

- [ ] **Step 3: Minimal implementation**

In `score_build.py`, rename export:

```python
ML_FEATURE_KEYS = (
    "final_stop_confidence",
    "sequence_score",
    "dwell_fraction",
    "abuse_score",
    "theft_score",
)
# keep using ML_FEATURE_KEYS everywhere _ML_FEATURE_KEYS was used
```

In `assess_order` signature add `feature_sink: dict[str, float] | None = None`. After `result = run_score_stage(ctx)`:

```python
    if feature_sink is not None:
        feature_sink.update({k: float(ctx.ml_feature_vec[k]) for k in ML_FEATURE_KEYS})
```

Import `ML_FEATURE_KEYS` from `score_build`.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_train_synth.py::test_assess_feature_sink -v`  
Expected: PASS

---

### Task 2: Scenario templates + builders

**Files:**
- Create: `src/offline_cancel_risk/train/__init__.py`
- Create: `src/offline_cancel_risk/train/scenarios.py`
- Test: `tests/test_train_synth.py`

**Interfaces:**
- Produces: `TEMPLATES: dict[str, tuple[int, int, int]]` → labels `(offline, abuse, theft)`
- Produces: `DEFAULT_WEIGHTS: dict[str, float]`
- Produces: `ScenarioRow` dataclass: `template`, `request: AssessRequest`, `points: list[GpsPoint]`, `labels: dict[str, int]`
- Produces: `draw_template(rng, weights) -> str`
- Produces: `build_scenario(template: str, *, order_display_id: str, driver_id: int, rng) -> ScenarioRow`

- [ ] **Step 1: Write failing tests**

```python
from offline_cancel_risk.train.scenarios import (
    DEFAULT_WEIGHTS, TEMPLATES, build_scenario, draw_template,
)
import numpy as np

def test_template_labels_and_weights():
    assert abs(sum(DEFAULT_WEIGHTS.values()) - 1.0) < 1e-9
    assert TEMPLATES["theft_dwell"] == (0, 0, 1)
    assert TEMPLATES["plain_offline"] == (1, 0, 0)
    assert TEMPLATES["abuse_chain"] == (0, 1, 0)
    assert TEMPLATES["clean_cancel"] == (0, 0, 0)
    assert TEMPLATES["gps_sparse"] == (0, 0, 0)

def test_build_scenario_shapes():
    rng = np.random.default_rng(0)
    for name in TEMPLATES:
        row = build_scenario(name, order_display_id=f"O-{name}", driver_id=9, rng=rng)
        assert row.template == name
        assert row.request.order_display_id == f"O-{name}"
        offline, abuse, theft = TEMPLATES[name]
        assert row.labels == {
            "cancelled_offline": offline,
            "cancel_abuse": abuse,
            "selective_theft": theft,
        }
        if name == "gps_sparse":
            assert len(row.points) < 5
        else:
            assert len(row.points) >= 10
```

- [ ] **Step 2: Run tests — expect FAIL (import error)**

Run: `pytest tests/test_train_synth.py::test_template_labels_and_weights tests/test_train_synth.py::test_build_scenario_shapes -v`

- [ ] **Step 3: Implement `scenarios.py`**

```python
# src/offline_cancel_risk/train/scenarios.py
from __future__ import annotations
from dataclasses import dataclass
from datetime import datetime, timedelta
from typing import Any

import numpy as np

from offline_cancel_risk.api.schemas import AssessRequest
from offline_cancel_risk.domain.models import GpsPoint

HEADS = ("cancelled_offline", "cancel_abuse", "selective_theft")

TEMPLATES: dict[str, tuple[int, int, int]] = {
    "theft_dwell": (0, 0, 1),
    "plain_offline": (1, 0, 0),
    "abuse_chain": (0, 1, 0),
    "clean_cancel": (0, 0, 0),
    "gps_sparse": (0, 0, 0),
}

DEFAULT_WEIGHTS: dict[str, float] = {
    "theft_dwell": 0.20,
    "plain_offline": 0.25,
    "abuse_chain": 0.15,
    "clean_cancel": 0.30,
    "gps_sparse": 0.10,
}

PICKUP = (14.5500, 121.0200)
DEST = (14.6500, 121.0800)

@dataclass(frozen=True)
class ScenarioRow:
    template: str
    request: AssessRequest
    points: list[GpsPoint]
    labels: dict[str, int]

def draw_template(rng: np.random.Generator, weights: dict[str, float] | None = None) -> str:
    w = weights or DEFAULT_WEIGHTS
    names = list(w.keys())
    p = np.array([w[n] for n in names], dtype=float)
    p = p / p.sum()
    return str(rng.choice(names, p=p))

def _cluster(base: datetime, n: int, lat: float, lon: float, *, jitter: float, speed: float) -> list[GpsPoint]:
    pts: list[GpsPoint] = []
    for i in range(n):
        pts.append(GpsPoint(
            lat=lat + (i % 5) * jitter,
            lon=lon + (i % 3) * jitter,
            ts=(base + timedelta(minutes=i * 2)).strftime("%Y-%m-%d %H:%M:%S"),
            speed_mps=speed,
        ))
    return pts

def build_scenario(
    template: str,
    *,
    order_display_id: str,
    driver_id: int,
    rng: np.random.Generator,
) -> ScenarioRow:
    if template not in TEMPLATES:
        raise ValueError(f"unknown template: {template}")
    offline, abuse, theft = TEMPLATES[template]
    labels = {
        "cancelled_offline": offline,
        "cancel_abuse": abuse,
        "selective_theft": theft,
    }
    base = datetime(2024, 1, 1, 10, 0, 0)
    # jitter seed so rows differ slightly
    j = float(rng.uniform(0.000005, 0.00002))
    assign_ts = base.strftime("%Y-%m-%d %H:%M:%S")
    cancel_ts = (base + timedelta(hours=1, minutes=20)).strftime("%Y-%m-%d %H:%M:%S")
    latlong = f"{PICKUP[0]}|{PICKUP[1]},{DEST[0]}|{DEST[1]}"
    kwargs: dict[str, Any] = dict(
        order_display_id=order_display_id,
        driver_id=driver_id,
        cancel_ts=cancel_ts,
        assign_ts=assign_ts,
        latlong=latlong,
        path_point_num=2,
        order_status="CANCELLED",
        category="FOOD",
        order_value=800.0 if template == "theft_dwell" else 120.0,
        currency="PHP",
        next_driver_no_order=template in {"theft_dwell", "plain_offline"},
        region_code="PH",
        city_code="MNL",
    )
    if template == "theft_dwell":
        points = _cluster(base, 40, PICKUP[0], PICKUP[1], jitter=j, speed=0.2)
        kwargs["order_value"] = 800.0
        kwargs["category"] = "FOOD"
    elif template == "plain_offline":
        points = _cluster(base, 25, PICKUP[0], PICKUP[1], jitter=j, speed=0.3)
        kwargs["order_value"] = 50.0
    elif template == "abuse_chain":
        points = _cluster(base, 20, PICKUP[0], PICKUP[1], jitter=j, speed=1.0)
        kwargs["reassign_cancel_events"] = [
            {"ts": (base + timedelta(minutes=10)).strftime("%Y-%m-%d %H:%M:%S"), "driver_id": driver_id},
            {"ts": (base + timedelta(minutes=20)).strftime("%Y-%m-%d %H:%M:%S"), "driver_id": driver_id},
            {"ts": (base + timedelta(minutes=30)).strftime("%Y-%m-%d %H:%M:%S"), "driver_id": driver_id},
        ]
        kwargs["order_value"] = 200.0
    elif template == "clean_cancel":
        # brief track near pickup then cancel early — low dwell
        points = _cluster(base, 12, PICKUP[0] + 0.01, PICKUP[1] + 0.01, jitter=j, speed=5.0)
        kwargs["order_value"] = 80.0
        kwargs["next_driver_no_order"] = False
        cancel_ts = (base + timedelta(minutes=15)).strftime("%Y-%m-%d %H:%M:%S")
        kwargs["cancel_ts"] = cancel_ts
    else:  # gps_sparse
        points = _cluster(base, 2, PICKUP[0], PICKUP[1], jitter=j, speed=0.0)
        kwargs["order_value"] = 90.0
    req = AssessRequest(**kwargs)
    return ScenarioRow(template=template, request=req, points=points, labels=labels)
```

`train/__init__.py` can be empty or re-export `HEADS`.

- [ ] **Step 4: Run tests — expect PASS**

Run: `pytest tests/test_train_synth.py::test_template_labels_and_weights tests/test_train_synth.py::test_build_scenario_shapes -v`

---

### Task 3: Phase B label flip helper

**Files:**
- Create: `src/offline_cancel_risk/train/labels.py`
- Test: `tests/test_train_synth.py`

**Interfaces:**
- Produces: `flip_labels(labels: dict[str, int], rng, flip_rate: float) -> dict[str, int]`
- Produces: `teacher_labels_from_flags(flags: dict[str, int]) -> dict[str, int]`

- [ ] **Step 1: Failing tests**

```python
from offline_cancel_risk.train.labels import flip_labels, teacher_labels_from_flags

def test_teacher_labels_from_flags():
    assert teacher_labels_from_flags({"cancelled_offline": 1, "cancel_abuse": 0, "selective_theft": 1}) == {
        "cancelled_offline": 1, "cancel_abuse": 0, "selective_theft": 1,
    }

def test_flip_labels_rate():
    rng = np.random.default_rng(0)
    base = {"cancelled_offline": 0, "cancel_abuse": 0, "selective_theft": 0}
    flips = 0
    n = 5000
    for _ in range(n):
        out = flip_labels(base, rng, flip_rate=0.1)
        flips += sum(1 for k in base if out[k] != base[k])
    rate = flips / (n * 3)
    assert 0.07 < rate < 0.13
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Implement**

```python
# src/offline_cancel_risk/train/labels.py
from __future__ import annotations
import numpy as np
from offline_cancel_risk.train.scenarios import HEADS

def teacher_labels_from_flags(flags: dict[str, int]) -> dict[str, int]:
    return {h: int(flags[h]) for h in HEADS}

def flip_labels(
    labels: dict[str, int],
    rng: np.random.Generator,
    *,
    flip_rate: float,
) -> dict[str, int]:
    if not 0.0 <= flip_rate <= 1.0:
        raise ValueError("flip_rate must be in [0, 1]")
    out = dict(labels)
    for h in HEADS:
        if rng.random() < flip_rate:
            out[h] = 1 - int(out[h])
    return out
```

- [ ] **Step 4: Run — expect PASS**

---

### Task 4: Shard I/O + manifest

**Files:**
- Create: `src/offline_cancel_risk/train/shards.py`
- Test: `tests/test_train_synth.py`

**Interfaces:**
- Produces: `FEATURE_KEYS` alias of `ML_FEATURE_KEYS`
- Produces: `write_shard(outdir, shard_idx, X, y, meta_lines) -> Path`
- Produces: `load_all_shards(outdir) -> tuple[np.ndarray, np.ndarray]`  # X (n,5), y (n,3)
- Produces: `read_manifest(outdir) -> dict` / `write_manifest(outdir, data)`
- Produces: `next_shard_index(manifest) -> int`

Shard format:

```python
np.savez_compressed(
    path,
    X=X.astype(np.float32),          # (n, 5)
    y=y.astype(np.float32),          # (n, 3) order HEADS
    feature_names=np.array(FEATURE_KEYS),
    head_names=np.array(HEADS),
)
# optional meta jsonl alongside
```

Manifest keys: `phase`, `n_target`, `n_done`, `shard_size`, `seed`, `weights`, `flip_rate`, `policy_path`, `shards: list[str]`

- [ ] **Step 1: Failing test** — write shard, reload, assert shapes and values round-trip

- [ ] **Step 2: Implement `shards.py`** (stdlib + numpy only)

- [ ] **Step 3: PASS**

---

### Task 5: Generate + assess one shard

**Files:**
- Create: `src/offline_cancel_risk/train/dataset.py`
- Test: `tests/test_train_synth.py`

**Interfaces:**
- Consumes: `build_scenario`, `draw_template`, `assess_order`+`feature_sink`, Phase B helpers
- Produces: `async def generate_shard(*, phase: str, n: int, start_index: int, seed: int, weights, flip_rate, policy, outdir, shard_idx) -> dict`
  - Returns `{"n": n, "path": str, "seconds": float}`
  - Writes npz + updates manifest `n_done`

Logic:

```python
async def generate_shard(...):
    rng = np.random.default_rng(seed + shard_idx)
    Xs, ys, metas = [], [], []
    # shared temp publishers for the shard
    with TemporaryDirectory() as tmp:
        stream = JsonlStreamPublisher(...)
        table = SqliteTablePublisher(...)  # unique order ids → no idempotency collision
        t0 = time.perf_counter()
        for i in range(n):
            idx = start_index + i
            template = draw_template(rng, weights)
            row = build_scenario(template, order_display_id=f"SYN-{phase}-{idx}", driver_id=1000 + (idx % 50000), rng=rng)
            sink = {}
            result = await assess_order(row.request, FakeGpsClient(row.points), policy, stream=stream, table=table, feature_sink=sink)
            if phase == "a":
                labels = row.labels
            else:
                labels = flip_labels(teacher_labels_from_flags(result.flags.model_dump()), rng, flip_rate=flip_rate)
            Xs.append([sink[k] for k in ML_FEATURE_KEYS])
            ys.append([labels[h] for h in HEADS])
            metas.append({"order_display_id": row.request.order_display_id, "template": template})
        path = write_shard(...)
    # update manifest
    return {...}
```

- [ ] **Step 1: Test** `generate_shard` with `n=5`, phase `a`, assert npz exists and `manifest["n_done"]==5`

- [ ] **Step 2: Implement**

- [ ] **Step 3: PASS** (`pytest tests/test_train_synth.py::test_generate_shard_phase_a -v`)

---

### Task 6: Fit + emit bundle

**Files:**
- Create: `src/offline_cancel_risk/train/fit.py`
- Test: `tests/test_train_synth.py`

**Interfaces:**
- Produces: `fit_bundle(outdir_shards, bundle_dir, *, holdout_frac=0.05, seed=0) -> dict` metrics
- Bundle layout matches `load_bundle` / test fixtures:
  - `feature_schema.json` version `v1`, five float features
  - `model.joblib` = `{"model": MultiOutputRegressor(...), "heads": list(HEAD_NAMES)}`
  - `model.json` with checksum
  - `metrics_baseline.json` with per-head MAE or thresholded P/R on holdout

Use `HistGradientBoostingRegressor` if available in sklearn; fallback `Ridge`. Clamp is not required in estimator (blend clamps later).

```python
from sklearn.ensemble import HistGradientBoostingRegressor
from sklearn.multioutput import MultiOutputRegressor
from offline_cancel_risk.models.bundle import HEAD_NAMES, load_bundle

def fit_bundle(shard_dir: Path, bundle_dir: Path, *, holdout_frac: float = 0.05, seed: int = 0) -> dict:
    X, y = load_all_shards(shard_dir)
    rng = np.random.default_rng(seed)
    n = X.shape[0]
    idx = rng.permutation(n)
    n_hold = max(1, int(n * holdout_frac))
    hold, train = idx[:n_hold], idx[n_hold:]
    model = MultiOutputRegressor(
        HistGradientBoostingRegressor(max_depth=6, max_iter=100, random_state=seed)
    )
    model.fit(X[train], y[train])
    pred = np.clip(model.predict(X[hold]), 0, 1)
    # threshold 0.5 for P/R vs y[hold]
    ...
    # write bundle files + sha256
    handle = load_bundle(bundle_dir)  # smoke
    return metrics
```

- [ ] **Step 1: Test** — generate n=40 phase a, `fit_bundle`, `load_bundle(bundle).predict({...})` returns 3 heads in `[0,1]`-ish

- [ ] **Step 2: Implement `fit.py`** (mirror checksum writing from `tests/test_model_bundle.py::_write_bundle`)

- [ ] **Step 3: PASS**

---

### Task 7: CLI `scripts/train_synth.py`

**Files:**
- Create: `scripts/train_synth.py`
- Modify: `docs/OPS.md` (short section)

**Interfaces:**
- CLI flags: `--phase {a,b}`, `--n`, `--outdir`, `--shard-size` (default 10000), `--seed`, `--start-shard`, `--flip-rate`, `--train-only`, `--bundle-dir`, `--sideload-shadow`, `--policy`

Behavior:

1. Load/create manifest in `outdir`.
2. While `n_done < n`: `await generate_shard(...)`; log rows/sec + ETA.
3. Unless `--skip-train`: `fit_bundle` → `bundle_dir` default `data/models/synth-phase-{a|b}-{utc}`.
4. If `--sideload-shadow`: `ModelRegistry(settings...).sideload(bundle_dir, role="shadow")`.

```python
# scripts/train_synth.py
async def main_async(args):
    ...
def main():
    parser = argparse.ArgumentParser(...)
    asyncio.run(main_async(parser.parse_args()))
```

OPS blurb under install/run:

```markdown
### Synthetic model train (overnight)

Phase A then B (~500k full assess each):

```bash
python scripts/train_synth.py --phase a --n 500000 --outdir data/train/phase_a --seed 7
python scripts/train_synth.py --phase b --n 500000 --outdir data/train/phase_b --seed 11 --flip-rate 0.05 --sideload-shadow
```

Resume with `--start-shard N` or re-run same `--outdir` (manifest `n_done`).
```

- [ ] **Step 1: Manual smoke**

Run: `python scripts/train_synth.py --phase a --n 30 --outdir /tmp/ocr-train-a --shard-size 10 --seed 1`  
Expected: 3 shards (or 2+remainder), bundle under `data/models/...` or `--bundle-dir`, exits 0

- [ ] **Step 2: `pytest tests/test_train_synth.py -q` PASS**

- [ ] **Step 3: Full suite** `pytest -q` still green

---

### Task 8: Kick overnight Phase A (ops, not code)

- [ ] **Step 1: Start Phase A in background with logging**

```bash
mkdir -p data/train/phase_a logs
nohup python scripts/train_synth.py --phase a --n 500000 \
  --outdir data/train/phase_a --shard-size 10000 --seed 7 \
  --bundle-dir data/models/synth-phase-a \
  > logs/train_phase_a.log 2>&1 &
echo $!
```

- [ ] **Step 2: Verify first shard lands within ~first hour** — `ls data/train/phase_a/` and `tail logs/train_phase_a.log`

- [ ] **Step 3: After Phase A completes, start Phase B** (same pattern, `--phase b`, `--flip-rate 0.05`, `--sideload-shadow`)

---

## Spec coverage check

| Spec item | Task |
|---|---|
| Scenario templates + weights | 2 |
| Full assess + feature keys | 1, 5 |
| npz shards + manifest resume | 4, 5, 7 |
| MultiOutputRegressor bundle | 6 |
| Phase B noisy teacher | 3, 5 |
| CLI + train-only | 7 |
| Shadow sideload | 7 |
| OPS overnight | 7–8 |
| Tiny tests | 1–6 |
| No parquet / no workers | honored |

## Placeholder scan

None intentional. GPS geometry is approximate for template intent (not golden-score locked).
